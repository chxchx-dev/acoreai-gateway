FROM node:20.19.5-alpine3.22

WORKDIR /app

RUN corepack enable
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

EXPOSE 5175

CMD ["sh", "-lc", "if [ ! -d node_modules/vite ]; then pnpm install --frozen-lockfile; fi && pnpm run dev -- --host 0.0.0.0 --port 5175"]
