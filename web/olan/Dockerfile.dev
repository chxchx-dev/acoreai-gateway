FROM node:20.19.5-alpine3.22

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 5176

CMD ["sh", "-lc", "if [ ! -d node_modules/vite ]; then npm ci; fi && npm run dev -- --host 0.0.0.0 --port 5176"]
