export enum UserRole {
  FREE = 'FREE',
  ACADEMIC = 'ACADEMIC',
  PLUS = 'PLUS',
  ADMIN = 'ADMIN',
  /** Requests that come from trusted OLAN apps — no plan restrictions applied */
  APP = 'APP',
}
